This is just a basic sample to demonstrate operation of client and server.

This sample allow only one client to send data to server at a time.

The direction of communication is only client => server.